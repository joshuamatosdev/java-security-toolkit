# Supply Chain Testkit

Reusable contracts for [`supply-chain`](../supply-chain/) policies. Adopters can run the same
action-pin, base-image, wrapper, and trust-anchor expectations against their own repository inputs.

Depend on this module in test scope and implement the applicable contracts.

Verify with `./gradlew :supply-chain-testkit:test`.
