{
  "properties": [
    {
      "name": "tenant.isolation.enabled",
      "type": "java.lang.Boolean",
      "sourceType": "io.github.joshuamatosdev.security.tenant.spring.TenantIsolationAutoConfiguration",
      "description": "Whether the tenant-isolation auto-configuration activates and provides the tenant-aware primary DataSource (placement routing plus the signed session claim bound on every borrow).",
      "defaultValue": true
    }
  ]
}
