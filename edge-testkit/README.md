# Edge Testkit

Reusable contracts for applications adopting [`edge`](../edge/). The contracts verify
credentialed CORS origin rejection and hardened cookie defaults without requiring an end-to-end
identity provider.

Depend on this module in test scope and implement the relevant contract interfaces.

Verify with `./gradlew :edge-testkit:test`.
